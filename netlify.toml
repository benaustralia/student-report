[build]
  publish = "dist"
  command = "bun install && bun run build && cd netlify/functions && npm install"
  functions = "netlify/functions"

[functions]
  included_files = ["netlify/functions/data/**"]

[build.environment]
  NODE_VERSION = "20"

# Production branch settings
[context.production]
  command = "bun install && bun run build && cd netlify/functions && npm install"

# Development branch settings  
[context.development]
  command = "bun install && bun run build && cd netlify/functions && npm install"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
